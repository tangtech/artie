<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Tang Technical", root_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <% if signed_in? %>
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <%= current_user.email %> <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Change Password", edit_user_path(current_user) %></li>
                  <li><%= link_to "Sign Out",        signout_path, method: "delete" %></li>
                </ul>
              </li>
              <% if current_user.admin? %>
                  <li id="fat-menu" class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                      Site Administration <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                      <li><%= link_to "List All Users",  users_path %></li>
                      <li><%= link_to "Add New User", createuser_path %></li>
                      <li class="divider"></li>
                      <li><%= link_to "List All Customers",  customers_path %></li>
                      <li><%= link_to "Add New Customer", createcustomer_path %></li>
                    </ul>
                  </li>
              <% end %>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>
