<% provide(:title, 'Add New Customer') %>
<h1>Add New Customer</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@customer) do |f| %>
        <%= render 'fields', f: f %>
        <%= f.submit "Add New Customer", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>